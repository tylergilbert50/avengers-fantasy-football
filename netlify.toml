[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# SPA fallback. Listed after the functions' own `config.path` routes, which
# Netlify matches first.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
